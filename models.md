# Comandos para as classes do models

Cada classe no arquivo models represnta uma tabela na base da dados.
Apos alterar alguma das classe é necessário rodar os comandos, no terminal: 
```sh
manage.py makemigrations
manage.py migrate
```